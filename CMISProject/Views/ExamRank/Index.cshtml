@model IEnumerable<CMISProject.Models.ExamRank>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Percentage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotalMarks)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rank)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SGPA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Percentage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TotalMarks)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rank)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SGPA)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ExamRankId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ExamRankId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ExamRankId })
        </td>
    </tr>
}

</table>

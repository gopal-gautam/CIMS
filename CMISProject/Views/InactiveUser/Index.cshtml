@model IEnumerable<CMISProject.Models.InactiveUser>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.InactiveDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Reason)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.InactiveDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Reason)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InactiveUserId }) |
            @Html.ActionLink("Details", "Details", new { id=item.InactiveUserId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InactiveUserId })
        </td>
    </tr>
}

</table>
